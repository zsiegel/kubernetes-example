kind: Service
apiVersion: v1
metadata:
  name: zcorp
spec:
  type: NodePort
  selector:
    app: zcorp
    tier: app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http